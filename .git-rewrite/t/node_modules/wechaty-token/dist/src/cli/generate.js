"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generate = void 0;
/* eslint-disable sort-keys */
const cmd_ts_1 = require("cmd-ts");
const wechaty_token_1 = require("../wechaty-token");
async function handler(args) {
    try {
        const result = await wechaty_token_1.WechatyToken.generate(args.type);
        console.error(result);
    }
    catch (e) {
        console.error(e);
    }
}
const generate = cmd_ts_1.command({
    name: 'generate',
    description: 'Generate a new Wechaty Token',
    args: {
        type: cmd_ts_1.option({
            description: 'The type of the Wechaty Puppet Service',
            long: 'type',
            short: 't',
            type: cmd_ts_1.optional(cmd_ts_1.string),
        }),
    },
    handler,
});
exports.generate = generate;
//# sourceMappingURL=generate.js.map