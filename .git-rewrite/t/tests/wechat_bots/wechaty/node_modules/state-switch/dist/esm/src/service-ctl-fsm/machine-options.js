const buildMachineOptions = (options) => {
    const reset = async () => {
        await options.stop();
        await options.start();
    };
    return {
        actions: {
        // onEntry,
        // onExit,
        },
        activities: {},
        delays: {},
        guards: {},
        services: {
            reset,
            start: options.start,
            stop: options.stop,
        },
    };
};
export { buildMachineOptions, };
//# sourceMappingURL=machine-options.js.map