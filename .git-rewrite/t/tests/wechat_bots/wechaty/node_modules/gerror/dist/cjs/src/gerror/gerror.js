"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isEcmaError = exports.isGrpcStatus = exports.isGError = exports.GError = void 0;
const ecma_js_1 = require("./ecma.js");
Object.defineProperty(exports, "isEcmaError", { enumerable: true, get: function () { return ecma_js_1.isEcmaError; } });
const grpc_js_1 = require("./grpc.js");
Object.defineProperty(exports, "isGrpcStatus", { enumerable: true, get: function () { return grpc_js_1.isGrpcStatus; } });
const puppet_js_1 = require("./puppet.js");
const isGError = (payload) => payload instanceof Object
    && (0, ecma_js_1.isEcmaError)(payload)
    && (0, grpc_js_1.isGrpcStatus)(payload);
exports.isGError = isGError;
class GError extends Error {
    constructor(payload) {
        super();
        /**
         * Common properties
         */
        this.message = payload.message || '';
        if (isGError(payload)) {
            this.code = payload.code;
            this.details = payload.details;
            this.name = payload.name;
            this.stack = payload.stack;
        }
        else if ((0, grpc_js_1.isGrpcStatus)(payload)) {
            this.code = payload.code;
            this.details = payload.details;
            /**
             * Convert gRPC error to EcmaError
             */
            this.name = Array.isArray(payload.details) && payload.details.length > 0
                ? payload.details[0]
                : grpc_js_1.Code[this.code] || String(this.code);
        }
        else if ((0, ecma_js_1.isEcmaError)(payload)) {
            this.name = payload.name;
            this.stack = payload.stack;
            /**
             * Convert EcmaError to gRPC error
             */
            this.code = grpc_js_1.Code.UNKNOWN;
            this.details = payload.stack;
        }
        else {
            throw new Error('payload is neither EcmaError nor GrpcStatus');
        }
    }
    static stringify(payload) {
        return JSON.stringify(this.from(payload));
    }
    /**
     * GError.from() supports:
     *  - string
     *  - Error instance
     *  - GError instance
     *  - GrpcStatus object
     *  - EcmaError object
     *  - GError object
     *  - PuppetEventErrorPayload object/string
     */
    static from(payload) {
        /**
         * Huan(202110): in case of the payload is a Puppet Error Payload
         *  CAUTION: we must make sure the payload with a { data } property
         *    can be only the EventErrorPayload
         */
        if ((0, puppet_js_1.isPuppetEventErrorPayload)(payload)) {
            payload = payload.data;
        }
        /**
         * Error, GError, GrpcStatus, EcmaError, GError, and string
         */
        if (payload instanceof Object || typeof payload === 'string') {
            try {
                return this.fromJSON(payload);
            }
            catch (_) {
                // ignore any error and pass on
            }
        }
        /**
         * `string` that can not be parsed to JSON
         */
        if (typeof payload === 'string') {
            const e = new Error(payload);
            e.name = 'GError';
            return this.from(e);
        }
        /**
         * others
         */
        const payloadType = typeof payload;
        if (payload instanceof Object) {
            try {
                payload = JSON.stringify(payload);
            }
            catch (e) {
                payload = e.message;
            }
        }
        const e = new Error(`${payload}`);
        e.name = 'GError: from(`' + payloadType + '`)';
        return this.from(e);
    }
    /**
     * From a gRPC standard error
     *  Protobuf
     */
    static fromJSON(payload) {
        if (typeof payload === 'string') {
            payload = JSON.parse(payload);
        }
        if (!(0, ecma_js_1.isEcmaError)(payload) && !(0, grpc_js_1.isGrpcStatus)(payload)) {
            throw new Error('payload is neither EcmaError nor GrpcStatus');
        }
        const e = new this(payload);
        return e;
    }
    toJSON() {
        return {
            code: this.code,
            details: this.details,
            message: this.message,
            name: this.name,
            stack: this.stack,
        };
    }
    toGrpcStatus() {
        return {
            code: this.code,
            details: this.details,
            message: this.message,
        };
    }
    toEcmaError() {
        return {
            message: this.message,
            name: this.name,
            stack: this.stack,
        };
    }
}
exports.GError = GError;
//# sourceMappingURL=gerror.js.map